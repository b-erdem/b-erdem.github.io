# Only needed for LOCAL preview (`bundle exec jekyll serve`).
# GitHub Pages builds the site server-side on push — you do NOT need Ruby
# installed to publish.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-feed"

# Lock to the platforms GitHub Pages uses, to avoid local/remote drift.
gem "webrick"
